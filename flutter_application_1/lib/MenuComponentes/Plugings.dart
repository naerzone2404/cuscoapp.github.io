import 'package:flutter/material.dart';

class Plugings extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('MisPlugings'),
          centerTitle: true,
          backgroundColor: Colors.pink,
        ),
      );
}
